configure_file(
    input: 'gftp.in',
    output: 'gftp',
    configuration: {
        'bindir': get_option('prefix') / get_option('bindir')
    },
    install: true,
    install_dir: get_option('bindir')
)